package com.example.proxy.core.server.handlers;

import com.example.proxy.core.server.handlers.ServerHandler;

/* 
 *  Overview: Handles HTTP/1.x requests including parsing headers, reading body,
 *            passing to routing layer
 */

public class Http1ServerHandler implements ServerHandler {
    
}